// Funkcja wywoływana po naciśnięciu przycisku "Wyświetl uczniów"
function pokazUczniow() {
    // Tutaj można umieścić kod, który wyświetli uczniów
    const main = document.querySelector('main');
    main.innerHTML = '<h2>Wyświetlanie uczniów</h2>';
}

// Funkcja wywoływana po naciśnięciu przycisku "Wyświetl oceny uczniów"
function pokazOceny() {
    // Tutaj można umieścić kod, który wyświetli oceny uczniów
    const main = document.querySelector('main');
    main.innerHTML = '<h2>Wyświetlanie ocen uczniów</h2>';
}

// Funkcja wywoływana po naciśnięciu przycisku "Wyświetl obecności"
function pokazObecnosci() {
    // Tutaj można umieścić kod, który wyświetli obecności
    const main = document.querySelector('main');
    main.innerHTML = '<h2>Wyświetlanie obecności</h2>';
}
